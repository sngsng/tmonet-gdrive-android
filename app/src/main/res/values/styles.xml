<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="LightToolbarStyle" parent="ThemeOverlay.AppCompat.Light">

    </style>

    <style name="AppProgressDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/progress_enter</item>
        <item name="android:windowExitAnimation">@anim/progress_exit</item>
    </style>

    <style name="ChargeDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/modal_open</item>
        <item name="android:windowExitAnimation">@anim/modal_close</item>
    </style>

</resources>
